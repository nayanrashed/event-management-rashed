import React from 'react';

const SingleServiceCard = ({selectedService}) => {

    const {id,picture,}=selectedService;
    return (
        <div>
            
        </div>
    );
};

export default SingleServiceCard;